# Job Monitor Agent Configuration Template
# Copy this file to config.env and fill in your values

# Database path
JOB_MONITOR_DB_PATH=/home/ubuntu/job_monitor/jobs.db

# Email Configuration (Outlook/Office 365)
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587

# Sender email credentials
# For Outlook, you may need to use an App Password instead of your regular password
# Generate one at: https://account.microsoft.com/security
SENDER_EMAIL=your-email@outlook.com
SENDER_PASSWORD=your-app-password-here

# Recipient email (where to send daily digests)
RECIPIENT_EMAIL=recipient@example.com

# OpenAI API Key (already configured in environment)
# OPENAI_API_KEY is automatically loaded from system environment
